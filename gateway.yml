server:
   port: 6000

eureka:
   client:
      serviceUrl:
         defaultZone: http://localhost:7777/eureka/

spring:
   application:
      name: "gateway"

zuul:
   routes:
      hello-service:
         url: http://localhost:8081

ribbon:
   eureka:
      enabled: false